apiVersion: v1
kind: Service
metadata:
  name: button-service
  namespace: prod
spec:
  selector:
    app: button
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
  